# Fixtures

Distributable example data. Nothing in this directory may contain real user
registry entries, local machine paths from a developer machine, tokens, or
private repository names.

- `oss-registry.example.json` — schema-valid example registry used by tests,
  documentation, and first-run validation. Fossight never ships this file as
  user seed data; a new installation starts with an empty registry.
